#include<bits/stdc++.h>
using namespace std;
int main()
{
    char c[150];
    cin>>c;
    int len = strlen(c);

    for(int i=0;i<len/2;i++)
    {
        if(c[i]!=c[len-1-i])
        {
            cout<<"No"<<endl;
            return 0;
        }
    }

    cout<<"Yes"<<endl;

    return 0;
}
